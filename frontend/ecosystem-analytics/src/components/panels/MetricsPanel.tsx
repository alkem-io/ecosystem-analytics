// This component has been superseded by the expanded MetricsBar.
// All detail views (rankings, connectors, multi-space users) are now
// rendered directly within MetricsBar's expandable section.
export default function MetricsPanel() {
  return null;
}
