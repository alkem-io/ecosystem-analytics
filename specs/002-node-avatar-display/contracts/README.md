# Contracts: Node Avatar Display

No API contract changes are required for this feature.

The existing BFF endpoint `GET /api/spaces/:id/generate` already returns `GraphNode.avatarUrl` in the dataset response. The change is purely in how the frontend renders these nodes visually.
