# Feature 025: only the fields the graph renders for a user (name, avatar, url,
# location, tags) plus `createdDate`, which the Explorer's temporal view places nodes by.
query usersByIDs($ids: [UUID!]!) {
  users(IDs: $ids) {
    id
    nameID
    createdDate
    profile {
      displayName
      avatar: visual(type: AVATAR) {
        uri
      }
      location {
        country
        city
        geoLocation {
          latitude
          longitude
        }
      }
      url
      tagsets {
        name
        tags
        type
      }
    }
  }
}
