query usersByIDs($ids: [UUID!]!) {
  users(IDs: $ids) {
    id
    nameID
    createdDate
    profile {
      displayName
      avatar: visual(type: AVATAR) {
        uri
      }
      location {
        country
        city
        geoLocation {
          latitude
          longitude
        }
      }
      url
      tagsets {
        name
        tags
        type
      }
    }
  }
}
